# build artifacts
app/build/
*.pyc
__pycache__/
.DS_Store
# runtime state lives in ~/.hermes — never here, but belt & braces
*.env
.env*
*.log
# Graphify generated artifacts (~5MB, rebuild with `graphify update .`)
graphify-out/

# release/build artifacts (release.yml packages into dist/; update.sh unpacks
# tarballs in a temp dir and leaves nothing here)
dist/
HermesAssistant-*.zip
HermesAssistant-*-source.tar.gz
SHA256SUMS
